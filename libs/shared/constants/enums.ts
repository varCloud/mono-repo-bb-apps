export const enum ENUM_STATUS_ACCOUNT {
  ACTIVE = 1,
  INACTIVE = 2,
  SUSPENDED = 3,
  DELETED = 4,
}

export enum ENUM_TYPE_USER {
  ATHLETE = 1,
  CREATOR = 2,
  PROSPECT = 3,
}

export const enum ENUM_STRIPE_STATUS_ACCOUNT {
  RESTRICTED = 'restricted',
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum ENUM_STATUS_MESSAGE {
  READ = 1,
  UNREAD = 2,
}

export enum ENUM_WORKOUT_TYPES {
  CLASS_VIDEO = 1,
  RUTINE_VIDEO = 2,
  DOCUMENT = 3,
}

export enum ENUM_WORKOUT_STATUS {
  PUBLISHED = 1,
  DRAFT = 2,
  DELETED = 3,
}

export enum MODAL_RESPONSE {
  CONFIRM = 'confirm',
  CANCEL = 'cancel',
  SUCCESS = 'success',
}

export enum BUCKET_TYPE {
  PUBLIC = 'public',
  PRIVATE = 'private',
}
